Create a new Next.js page at `src/pages/$ARGUMENTS.tsx` following the existing project pattern:

1. Use TypeScript with proper NextPage typing
2. Wrap content with ThemeTemplate layout
3. Use SectionWrapper and Container for consistent page structure
4. Add Framer Motion page transition animations matching existing pages
5. Use Tailwind CSS utility classes exclusively for styling
6. Include SEO metadata via `next/head` with appropriate title and description
7. Use `~/` path alias for all internal imports

Reference `src/pages/services.tsx` as the canonical example for page structure.
