Create a new React component at `src/components/$ARGUMENTS.tsx` following the project conventions:

1. TypeScript functional component with explicit prop interface
2. Use Tailwind CSS utility classes exclusively (no custom CSS)
3. Import with `~/` path alias for internal imports
4. Export as default
5. If animated, use Framer Motion (not CSS transitions)
6. Use classnames or clsx for conditional class logic if needed

Match the style and patterns of existing components in `src/components/`.
